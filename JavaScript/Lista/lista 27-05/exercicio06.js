//6 Escreva um código que remova todos os elementos de um array com shift() dentro de um while

const nomes = ["ana", "ester", "alicia"]
while((i = nomes.shift()) !== undefined) {
    //console.log(i)
}

console.log(nomes)
